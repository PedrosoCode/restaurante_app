import 'package:flutter/material.dart';

class UsuarioModePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Modo Usuário'),
      ),
      body: Center(
        child: Text('Página do Modo Usuário'),
      ),
    );
  }
}
